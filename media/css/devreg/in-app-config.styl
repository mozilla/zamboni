@import 'lib';

.devhub-form .in-app-config {
    input[type="text"] {
        width: 100%;
    }
    .is-https.hint {
        display: inline;
    }
    .preamble {
        margin: 1.5em 0;
        line-height: 1.3em;
    }
    table {
        margin-bottom: 26px;
    }
}

#in-app-private-key .secret {
    display: none;
}
