# These should all be in our wheelhouse to avoid compiling on production and
# automated testing machines

Jinja2==2.7.2
MarkupSafe==0.18
lxml==2.2.6
Pillow==2.3.0

# For JWT to sign App receipts:
# Temporary.
M2Crypto==0.22.3

# For pyjwkest, for JWT signing.
pycrypto==2.6.1

# To get SNI support in requests (needed by the app validator).
pyOpenSSL==0.13.1
